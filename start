# start up command for posix shell
#
# testing
printf "%s" "hello world"
exit 0
#| date
#
#
#
#if ! echo "Hello, world" > /tmp/test; then
#    echo "Error: failed to write to file" >&2
#    exit 1
#fi
#
#instance_root = ~/edit/$EDIT_INSTANCE
#
#if ! echo block > $instance_root/settings/cursor_semantics; then      # block | bar
#   echo "Error: failed to set cursor_semantics" >&2
#   exit 1
#fi
#
#if ! echo true > $instance_root/settings/use_full_file_path; then      # true | false
#   echo "Error: failed to set use_full_file_path" >&2
#   exit 1
#fi
#
#if ! echo false > /$instance_root/settings/use_hard_tab; then      # true | false  #indicates whether to use '\t' literal or spaces for indentation
#   echo "Error: failed to set use_hard_tab" >&2
#   exit 1
#fi
#
#if ! echo 4 > $instance_root/settings/tab_width; then      #the number of terminal cells to next tab stop
#   echo "Error: failed to set tab_width" >&2
#   exit 1
#fi
#
#if ! echo 1 > $instance_root/settings/view_scroll_amount; then      #the number of lines/columns to scroll
#   echo "Error: failed to set view_scroll_amount" >&2
#   exit 1
#fi
#
#if ! echo false > $instance_root/settings/show_cursor_column; then      # true | false    #indicates whether to highlight the whole column of the primary cursor (for visibility)
#   echo "Error: failed to set show_cursor_column" >&2
#   exit 1
#fi
#
#if ! echo true > $instance_root/settings/show_cursor_line; then      # true | false   #indicates whether to highlight the whole line of the primary cursor (for visibility)
#   echo "Error: failed to set show_cursor_line" >&2
#   exit 1
#fi
#
#if ! echo true > $instance_root/settings/show_line_numbers; then      # true | false
#   echo "Error: failed to set show_line_numbers" >&2
#   exit 1
#fi
#
#if ! echo true > $instance_root/settings/show_status_bar; then      # true | false
#   echo "Error: failed to set show_status_bar" >&2
#   exit 1
#fi




#TODO: remove the following...

#interactive modes like search/command/goto could prob be accomplished by an external dmenu like program interacting with our served filesystem...

#mouse wheel scrolling/clicking inside scroll bar/view movement keybinds modify the view(visible buffer text) displayed
#left click/cursor movement via keyboard navigate/extend the selection(s) inside the buffer
#middle click/execute key bind try to execute selected text as a command, and failing that, execute selected text as a shell command
#right click/plumb key bind behavior will attempt to plumb selected content to the plumber for handling.
        #current_file.extension:0:15        //navigate to line 0, column 15 in current file
        #current_file.extension:/regex/     //navigate to first occurance of matching regex in current file
        #external_file.extension            //open another file in new edit instance in a new window
        #some_directory/                    //open a new window with shell and working directory set to some_directory/ relative to edit instances working directory
        #ls(1)                              //open a new window displaying the man page for section 1 of ls
        #www.google.com                     //open a new window with a browser at google.com
    #if no matching plumb rule, selected text will be used as a search parameter, and edit will attempt to select the next instance of the selected text. //maybe ctrl+right click to search previous?...
    #if both operations fail, an error will be served in a diagnostic file (something along the lines of "no matching plumb rule or buffer text")

#commands could be scripts/program binaries located at ~/edit/bin. at startup edit can bind that directory to PATH
#can name collisions be avoided by binding before anything else, so that the first match is our command and not another util with same name?...
